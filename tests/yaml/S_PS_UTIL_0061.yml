pipelines:
  - name: pipeline_S_PS_UTIL_0061
    steps:
      - name: S_PS_UTIL_0061
        type: PowerShell
        configuration:
          nodePool: win_2019
          # outputResources:
          #   - name: outgoing

        execution:
          onExecute:
            - add_pipeline_variables testvariable="asdffdjksfj.       k455457457"
