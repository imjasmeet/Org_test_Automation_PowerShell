template: true
valuesFilePath: ../values/gitRepoValues.yml

resources:
  - name: S_PS_Tasks_10148_10_repo
    type: GitRepo
    configuration:
      gitProvider: s_gitHub
      path: {{ .Values.GitHub.Org_test_Automation_PowerShell.path }}
      branches:
        include: {{ gitBranch }}

pipelines:
  - name: S_PS_Tasks_10148_10_pipeline
    configuration:
      jfrogCliVersion: 2
      nodePool: win_2019
    steps:
      - name: S_PS_Tasks_10148_10_step
        type: PowerShell
        configuration:
          integrations:
            - name: s_artifactory
          inputResources:
            - name: S_PS_Tasks_10148_10_repo
              trigger: false
        execution:
          onStart:
            - Set-Variable -Name "taskVersion" -Scope "Global" -Value "v19.10.${run_id}"
            - New-Item -Path "$step_tmp_dir/$taskVersion" -ItemType Container
            - Copy-Item -Path "${res_S_PS_Tasks_10148_10_repo_resourcePath}/tests/tasks/hello/*" -Destination $step_tmp_dir/task/ -Recurse -Force
            - Push-Location $step_tmp_dir/
            - jf rt u "task/*" "test-automation-generic-local/jfrog/hello/${taskVersion}.zip" --archive zip
          onComplete:
            - echo "on Complete"
          onSuccess:
            - task: jfrog/hello@${taskVersion}
              repository: test-automation-generic-local
              id: S_PS_Tasks_10148_10_task
              input:
                target: "start"
            - echo "on Success"
            - try { jf rt del "test-automation-generic-local/jfrog/hello/${taskVersion}.zip" } catch {}

